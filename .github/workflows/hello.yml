name: Hello Workflow

on:
  push:

jobs:
  hello:
    runs-on: ubuntu-latest

    steps:
      - name: Print Hello
        run: echo "Hello from CI!"

      - name: Print Date
        run: date

      - name: Print Commit Hash
        run: echo "${{ github.sha }}"
